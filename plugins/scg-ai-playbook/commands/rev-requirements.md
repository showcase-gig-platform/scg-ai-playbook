# Name: rev-requirements

---

# Description: 既存のコードベースから要件定義書を逆生成します。EARS記法を用いて機能要件、非機能要件、ユーザーストーリーを抽出・文書化します。

---

# Content

## 目的

既存のコードベースから要件定義書を逆生成する。実装された機能を分析し、EARS（Easy Approach to Requirements Syntax）記法を用いて機能要件、非機能要件、ユーザーストーリーを抽出・文書化する。

## 前提条件

- 分析対象のコードベースが存在する
- `docs/reverse/` ディレクトリが存在する（なければ作成）
- 可能であれば事前に `/rev-tasks`, `/rev-design`, `/rev-specs` を実行済み

## 実行内容

1. **機能の特定と分析**
   - UIコンポーネントから画面機能を抽出
   - APIエンドポイントからビジネス機能を特定
   - データベーススキーマからデータ要件を推定
   - テストコードから期待動作を確認

2. **ユーザーストーリーの逆算**
   - 実装された機能からユーザーの意図を推定
   - WHO（ユーザー種別）の特定
   - WHAT（実現したいこと）の抽出
   - WHY（得られる価値）の推定

3. **EARS記法による要件分類**
   - **通常要件（SHALL）**: 標準的な機能実装から抽出
   - **条件付き要件（WHEN/IF-THEN）**: 条件分岐ロジックから抽出
   - **状態要件（WHERE）**: 状態管理実装から抽出
   - **オプション要件（MAY）**: 設定可能機能から抽出
   - **制約要件（MUST）**: バリデーション・制限ロジックから抽出

4. **非機能要件の推定**
   - パフォーマンス要件：実装されたキャッシュ、最適化から推定
   - セキュリティ要件：認証・認可実装から抽出
   - ユーザビリティ要件：UI/UX実装から抽出
   - 運用要件：ログ、監視実装から抽出

5. **Edgeケースの特定**
   - エラーハンドリング実装から異常系要件を抽出
   - バリデーション実装から境界値要件を抽出
   - テストケースから想定されるエラーケースを抽出

6. **受け入れ基準の生成**
   - 実装されたテストから受け入れ基準を逆算
   - 未実装のテストケースを推奨事項として提示

7. **ファイルの作成**
   - `docs/reverse/{プロジェクト名}-requirements.md` として保存

## 出力フォーマット例

```markdown
# {プロジェクト名} 要件定義書（逆生成）

## 分析概要

**分析日時**: {実行日時}
**対象コードベース**: {パス}
**抽出要件数**: {機能要件数}個の機能要件、{非機能要件数}個の非機能要件
**信頼度**: {分析の信頼度}%（実装カバレッジに基づく）

## システム概要

### 推定されたシステム目的
{実装された機能から推測されるシステムの目的}

### 対象ユーザー
{UIコンポーネントや機能から推定されるユーザー種別}

## ユーザストーリー

### ストーリー1: [ストーリー名]
- **である** [ユーザー種別] **として**
- **私は** [実現したいこと] **をしたい**
- **そうすることで** [得られる価値]

**実装根拠**: 
- [関連する実装ファイル/機能]

## 機能要件（EARS記法）

### 通常要件

#### REQ-001: [要件名]
システムは[機能]を提供しなければならない。

**実装根拠**: 
- [関連する実装ファイル/機能]

### 条件付き要件

#### REQ-101: [要件名]
[条件]の場合、システムは[動作]しなければならない。

**実装根拠**:
- [関連する実装ファイル/機能]

### 状態要件

#### REQ-201: [要件名]
[状態]にある場合、システムは[動作]しなければならない。

### オプション要件

#### REQ-301: [要件名]
システムは[オプション機能]してもよい。

### 制約要件

#### REQ-401: [要件名]
システムは[制約]しなければならない。

## 非機能要件

### パフォーマンス

#### NFR-001: [要件名]
{パフォーマンス要件}

**実装根拠**:
- [関連する実装/設定]

### セキュリティ

#### NFR-101: [要件名]
{セキュリティ要件}

### ユーザビリティ

#### NFR-201: [要件名]
{ユーザビリティ要件}

### 運用性

#### NFR-301: [要件名]
{運用要件}

## Edgeケース

### エラー処理

#### EDGE-001: [ケース名]
{エラーケースの説明}

**実装根拠**:
- [関連するエラーハンドリング実装]

### 境界値

#### EDGE-101: [ケース名]
{境界値ケースの説明}

## 受け入れ基準

### 実装済み機能テスト

- [x] [テスト項目]: [実装済み]
- [ ] [テスト項目]: [未実装]

### 推奨追加テスト

- [ ] [推奨テスト項目]

## 推定されていない要件

### 不明確な部分

以下の要件は実装から推定が困難なため、ステークホルダーとの確認が必要：

1. **ビジネス要件**
   - [不明確な点]

2. **運用要件**
   - [不明確な点]

### 推奨される次ステップ

1. **ステークホルダーインタビュー** - 推定された要件の確認
2. **ユーザビリティテスト** - 実際のユーザビリティ要件の確認

## 分析の制約事項

### 信頼度に影響する要因

- **コメント不足**: 開発者の意図を推定で補完
- **テストカバレッジ**: [%]% - 未テスト部分の要件は推定
- **ドキュメント不足**: 外部仕様書が存在しない

### 推定の根拠

- **強い根拠**: 実装 + テスト + 明確な動作
- **中程度の根拠**: 実装 + 部分的テスト
- **弱い根拠**: 実装のみ、推定で補完
```

## 要件抽出のポイント

### 機能要件の抽出元

| コードの特徴 | 推定される要件 |
|-------------|---------------|
| APIエンドポイント | ビジネス機能要件 |
| UIコンポーネント | ユーザーインターフェース要件 |
| データベーススキーマ | データ要件 |
| バリデーション実装 | 制約要件 |
| 条件分岐 | 条件付き要件 |

### 非機能要件の推定元

| コードの特徴 | 推定される要件 |
|-------------|---------------|
| 設定ファイル + ライブラリ | パフォーマンス・セキュリティ要件 |
| UI実装パターン | ユーザビリティ要件 |
| ログ・監視実装 | 運用要件 |
| テスト実装 | 品質要件 |

## 実行後の確認

- 抽出された要件数（機能要件・非機能要件）を表示
- 分析の信頼度と根拠の強さを報告
- 推定が困難な要件や確認が必要な項目を提示
- ステークホルダー確認のための質問リストを生成
- 次の推奨アクション（テスト追加、ドキュメント整備等）を提案

## リバースエンジニアリング完了

全てのリバースエンジニアリングステップが完了しました。生成されたドキュメント：

- `docs/reverse/{プロジェクト名}-discovered-tasks.md`
- `docs/reverse/{プロジェクト名}-architecture.md`
- `docs/reverse/{プロジェクト名}-dataflow.md`
- `docs/reverse/{プロジェクト名}-api-specs.md`
- `docs/reverse/{プロジェクト名}-database.md`
- `docs/reverse/{プロジェクト名}-test-specs.md`
- `docs/reverse/{プロジェクト名}-test-cases.md`
- `docs/reverse/{プロジェクト名}-requirements.md`
