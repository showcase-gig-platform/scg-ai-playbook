# Name: tdd-green

---

# Description: TDDのGreenフェーズを実行します。Redフェーズで作成したテストを通すための最小限の実装を行います。

---

# Content

## 目的

TDDのGreenフェーズを実行する。Redフェーズで作成したテストを通すための**最小限の実装**を行う。

## 信頼性レベル指示

実装コード作成時には、各実装内容について元の資料との照合状況を以下の信号でコメントしてください：

- 🟢 **青信号**: 元の資料を参考にしてほぼ推測していない場合
- 🟡 **黄信号**: 元の資料から妥当な推測の場合
- 🔴 **赤信号**: 元の資料にない推測の場合

## 実装の原則

- **テストが確実に通ること最優先**
- コードの美しさは二の次（次のRefactorフェーズで改善）
- 「とりあえず動く」レベルでOK
- ハードコーディングでも構わない
- 複雑なロジックは後回し、シンプルな実装を心がける
- 既存のテストがエラーになった場合は仕様を元に適切に修正する
- **NEVER**: 必要なテストのスキップ
- **NEVER**: 必要なテストの削除

## 実装時の日本語コメント要件

実装コードには以下の日本語コメントを必ず含めてください：

### 関数・メソッドレベルのコメント

```
/**
 * 【機能概要】: [この関数が何をするかを日本語で説明]
 * 【実装方針】: [なぜこのような実装方法を選んだかを説明]
 * 【テスト対応】: [どのテストケースを通すための実装かを明記]
 * 🟢🟡🔴 信頼性レベル: [この実装が元資料のどの程度に基づいているか]
 */
```

### 処理ブロックレベルのコメント

```
// 【入力値検証】: [入力値の妥当性をチェックする理由と方法] 🟢🟡🔴
// 【データ処理開始】: [メイン処理の開始を明示] 🟢🟡🔴
// 【結果返却】: [処理結果を返す理由と内容の説明]
```

## 段階的実装のガイドライン

1. **まず1つのテストケースだけ通す**
   - 複数テストの同時対応は複雑化を招くため避ける
   - 1つずつ確実に実装することで品質を担保

2. **最も簡単な方法で実装**
   - 複雑なアルゴリズムは後のリファクタで追加
   - 現段階では理解しやすさを最優先

3. **ファイルサイズを意識した実装**
   - 将来的に適切なサイズになるよう機能を分離
   - 長大な関数は小さな単位に分割

4. **コード品質基準の考慮**
   - lintやtypecheckでエラーが出ない実装を心がける
   - プロジェクトの既存フォーマットに合わせた実装

5. **他のテストケースは後回し**
   - TDDの原則に従い、1ステップずつ進める

6. **エラーハンドリングも最小限**
   - テストで要求される部分のみ実装

## 提供してください

1. **実装コード**: テストを通すコード（必須の日本語コメント付き）
2. **テスト実行結果**: 実際にテストが通ることの確認
3. **実装の説明**: どのような考えで実装したか
4. **課題の特定**: 現在の実装の問題点（リファクタ対象の明確化）

## 実行後の処理

実装完了後、以下を実行してください：

1. **メモファイル更新**: `docs/implements/{task_id}/{feature_name}-memo.md`ファイルのGreenフェーズセクションを更新
   - 実装方針、実装コード、テスト結果、課題・改善点を記録
2. 実装コードと設計内容を`docs/implements/{task_id}/{feature_name}-green-phase.md`に保存
3. **品質判定**: 以下の条件を確認
   - 全てのテストが成功していること
   - 実装がシンプルで理解しやすい
   - 明らかなリファクタリング箇所がある
   - 機能的な問題がない

## 品質判定基準

```
✅ 高品質:
- テスト結果: 全て成功
- 実装品質: シンプルかつ動作する
- リファクタ箇所: 明確に特定可能
- 機能的問題: なし
- コンパイルエラー: なし

⚠️ 要改善:
- テストの一部が失敗
- 実装が複雑すぎる
- リファクタ方針が不明
- 機能に懸念がある
- コンパイルエラーが存在
```

## 出力フォーマット

```
## 🟢 Green: テストがパスしました

**テストケース**: [TC-XXX] [テストケース名]
**実装ファイル**: [ファイルパス]

### 実装したコード
[実装コード]

### テスト実行結果
[テスト実行結果（PASS）]

**実装内容**: [何を実装したか]

✅ テストがパスしています

### リファクタリングの候補
- [改善すべき点1]
- [改善すべき点2]
```

## 次のステップ

`/tdd-refactor` でコードの品質を改善します。
